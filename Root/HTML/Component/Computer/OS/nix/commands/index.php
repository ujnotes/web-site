<div id='message'>
	<p>
		Short notes on common *nix commands.
	</p>
	<h2>Tail</h2>
	<p>
		<code class='inline'>tail</code> prints the last lines of a file. <code class='inline'>-f</code> follows new output as it is written.
	</p>
	<pre class='indent-c'><code class='block'>tail -f</code></pre>
	<h2>Move</h2>
	<p>
		Move a file.
	</p>
	<h2>Aliases</h2>
	<p>
		<code class='inline'>unalias</code> removes an alias.
	</p>
	<p>
		Prefix a command with <code class='inline'>\</code> to use the native command instead of an alias.
	</p>
	<h2>Delete</h2>
	<p>
		Delete a file.
	</p>
</div>

<?php require('../HTML/Fragment/Component_bottom.php') ?>
