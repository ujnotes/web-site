<div id='message'>
	<p>
		<code class='inline'>su</code> switches user: run as another user. The user changes.
	</p>
	<p>
		Caution: <code class='inline'>su</code> changes to another account, default root.
	</p>
	<h2>Login shell</h2>
	<pre class='indent-c'><code class='block'>su -</code></pre>
	<p>
		Starts a login shell (default root).
	</p>
	<h2>Command</h2>
	<p>
		<code class='inline'>-c</code> is the same as <code class='inline'>sudo</code>: run a command as the other user.
	</p>
</div>

<?php require('../HTML/Fragment/Component_bottom.php') ?>
