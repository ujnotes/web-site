<div id='message'>
	<p>
		<code class='inline'>sudo</code> runs with superuser privileges. The user remains the same. The user should be in the sudo-able list.
	</p>
	<p>
		Caution: <code class='inline'>sudo</code> is admin rights.
	</p>
	<h2>Root shell</h2>
	<p>
		<code class='inline'>-s</code> starts a new root shell.
	</p>
	<pre class='indent-c'><code class='block'>sudo -s</code></pre>
</div>

<?php require('../HTML/Fragment/Component_bottom.php') ?>
