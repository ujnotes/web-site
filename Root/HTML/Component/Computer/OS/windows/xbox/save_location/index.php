<div id='message'>
	<p>
		Xbox captures default to {Video}\Capture; change the path by moving the {Video} location.
	</p>
	<pre class='indent-c'><code class='block'>Defaults to `{Video}\Capture`.</code></pre>
	<pre class='indent-c'><code class='block'>Can only be changed by moving the `{Video}` location: Properties &gt; Move.</code></pre>
</div>

<?php require('../HTML/Fragment/Component_bottom.php') ?>
