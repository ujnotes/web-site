<div id='message'>
	<p>
		Git.
	</p>
	<pre class='indent-c'><code class='block'>rm -r one-of-the-directories</code></pre>
	<p>
		Removes local.
	</p>
	<pre class='indent-c'><code class='block'>rm rm -r --cached &lt;directory&gt;// keep local</code></pre>
	<pre class='indent-c'><code class='block'>commit -m "Remove build directory"</code></pre>
	<p>
		Push origin master.
	</p>
	<p>
		Also add the path to the .gitignore file.
	</p>
	<h2>at once</h2>
	<p>
		Stage.
	</p>
	<pre class='indent-c'><code class='block'>add -u</code></pre>
</div>

<?php require('../HTML/Fragment/Component_bottom.php') ?>
