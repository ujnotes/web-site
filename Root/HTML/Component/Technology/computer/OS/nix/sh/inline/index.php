<div id='message'>
	<p>
		Inline a command's output in a string with $(...).
	</p>
	<pre class='indent-c'><code class='block'>`$(...)` runs the inner command and inserts its output into the surrounding string.</code></pre>
	<pre class='indent-c'><code class='block'>echo "xyz $(date)"</code></pre>
</div>

<?php require('../HTML/Fragment/Component_bottom.php') ?>
