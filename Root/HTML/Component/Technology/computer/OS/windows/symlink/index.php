<div id='message'>
	<p>
		MkLink soft links are relative to the target; junctions are followed over the network; hard links share edits and are relative to CD.
	</p>
	<h2>MkLink</h2>
	<h4>Soft</h4>
	<pre class='indent-c'><code class='block'>Path is relative to `&lt;target&gt;`.</code></pre>
	<h4>Junction</h4>
	<p>
		Also followed over the network. When followed from a client, usually: no access — fail.
	</p>
	<h4>Hard</h4>
	<pre class='indent-c'><code class='block'>An edit reflects on both. Path is relative to `&lt;CD&gt;`.</code></pre>
</div>

<?php require('../HTML/Fragment/Component_bottom.php') ?>
