<div id='message'>
	<h2>Test local</h2>
	<h4>serve</h4>
	<pre class='indent-c'><code class='block'>serve --only functions</code></pre>
	<p>
		Will print the localhost url with port.
	</p>
	<p>
		Should be in parent directory.
	</p>
	<p>
		<strong>Issue.</strong> Exit server and change to project directory (parent) and retry.
	</p>
	<pre class='indent-c'><code class='block'>firebase serve --only hosting</code></pre>
</div>

<?php require('../HTML/Fragment/Component_bottom.php') ?>
